import Link from "next/link"; import { requireAdminPrincipal } from "../../_lib/auth"; import { listAdminRelations } from "../../_lib/data"; import styles from "../../admin.module.css";
export default async function RelationsPage() { const principal = await requireAdminPrincipal("/admin/relations"); const rows = await listAdminRelations(principal); return <><header className={styles.pageHeader}><div><span className={styles.eyebrow}>KNOWLEDGE GRAPH</span><h1>关系图谱</h1><p>历史影响、文本传递与主题共鸣采用不同语义，不能互相替代。</p></div><Link className={styles.primaryAction} href="/admin/relations/new">新建关系</Link></header><section className={styles.panel}><div className={styles.panelHeading}><h2>{rows.length} 条关系版本</h2><span>{principal.mode === "local-preview" ? "只读公开快照" : "数据库版本"}</span></div><div className={styles.tableWrap}><table><thead><tr><th>关系</th><th>端点</th><th>类型</th><th>证据</th><th>状态</th></tr></thead><tbody>{rows.map((row) => <tr key={row.versionId}><td><Link href={`/admin/relations/${row.versionId}`}>{row.title}</Link></td><td>{row.fromTitle} {row.directed ? "→" : "↔"} {row.toTitle}</td><td>{row.relationType}</td><td>{row.evidenceStatus}</td><td>{row.status}</td></tr>)}</tbody></table></div></section></>; }
