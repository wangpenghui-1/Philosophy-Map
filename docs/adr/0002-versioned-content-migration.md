# ADR 0002：版本化内容与影子迁移

状态：已采纳

## 决策

Git JSON在迁移期仍是公开内容基线。影子导入按稳定ID和版本幂等写入PostgreSQL，published版本不可原位修改。后台首先只创建candidate版本；发布后生成与旧前端兼容的静态投影。

## 质量边界

- candidate、edited和reviewed均不得进入公共投影或AI检索。
- 主题共鸣不能升级为历史影响，数据库和导入检查共同执行方向约束。
- 来源、定位和主张随不可变内容片段保存。
- 只有连续发布周期验证数据库投影与静态投影一致，才移除旧读取路径。
