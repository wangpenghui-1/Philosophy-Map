# ADR 0003：有据可查的AI与显式长期记忆

状态：已采纳

## 决策

AI先检索published内容，构造Evidence Packet，再调用供应商。供应商返回若没有合法证据标记，则退回抽取式回答。证据不足时拒答。OpenAI请求使用`store: false`，本地数据库保存用户可见消息、引用快照和用量，不保存隐藏推理。

长期记忆默认关闭。用户开启后，显式创建或确认的记忆才能进入confirmed状态；用户可查看、编辑和硬删除。系统不得推断政治立场、宗教身份、健康状况等敏感属性。
